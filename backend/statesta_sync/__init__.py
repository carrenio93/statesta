"""Statesta sync worker package.

Session 7 (plumbing + connectivity smoke test only). No ingestion/normalization
logic lives here yet — see docs/SYNC_INGESTION_DESIGN.md for the design that the
later steps will implement.
"""
